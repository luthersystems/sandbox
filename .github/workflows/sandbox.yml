name: luthersystems/sandbox/luthersystems/sandbox
on:
  push:
    branches:
    - main
env:
  LUTHER_LICENSE: xxxxTT0K
jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v3.5.0
    - run: echo $LUTHER_LICENSE | base64 -d > .luther-license.yaml
    - run: make citest
